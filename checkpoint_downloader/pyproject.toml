[project]
name = "checkpoint-downloader"
version = "0.1.0"
description = "Download Hugging Face model snapshots into ../checkpoints"
requires-python = ">=3.11"
dependencies = [
    "huggingface-hub>=0.34",
    "hf-xet>=1.1",
]
